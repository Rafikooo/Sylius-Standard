{% set form = hookable_metadata.context.form %}
{#{{ dump(form.gatewayConfig) }}#}
<div class="col-12 col-md-6">
    {{ form_row(form.gatewayConfig.config.publishable_key, sylius_test_form_attribute('factory-name')|merge({ label_attr: { class: 'checkbox-switch' } })) }}
    {{ form_row(form.gatewayConfig.config.secret_key, sylius_test_form_attribute('factory-name')|merge({ label_attr: { class: 'checkbox-switch' } })) }}
</div>
